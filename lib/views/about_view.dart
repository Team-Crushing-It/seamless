import 'package:flutter/material.dart';

class AboutView extends StatelessWidget {
  final GlobalKey key;

  AboutView({@required this.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 1000, child: Text('abouts test2'));
  }
}
