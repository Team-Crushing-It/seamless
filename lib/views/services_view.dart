import 'package:flutter/material.dart';

class ServicesView extends StatelessWidget {
  final GlobalKey key;

  ServicesView({@required this.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 1000, child: Text('services test2'));
  }
}
